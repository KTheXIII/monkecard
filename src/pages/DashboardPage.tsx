import React, {
  useState,
  useEffect,
  useMemo,
} from 'react'

interface Props {
  isLoading: boolean
}

export const DashboardPage: React.FC<Props> = (props) => {
  return (
    <div></div>
  )
}

