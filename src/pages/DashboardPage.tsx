import React, {
  useState,
  useEffect,
  useMemo,
} from 'react'

import { MonkeUser } from '@scripts/user'

interface Props {
  isLoading: boolean
  user: MonkeUser
}

export const DashboardPage: React.FC<Props> = (props) => {
  return (
    <div></div>
  )
}

