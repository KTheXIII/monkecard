import React from 'react'
import { render } from '@testing-library/react'
import { assert, expect } from 'chai'
import { App } from './App'

describe('Useless Test', () => {
  it('test nothing', () => {
    assert(true, 'This is a very good test')
  })
})
